<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Category items</title>
</head>
<body>
        <% if (items[0]) { %>
            <ul>
            <% items.forEach(item => { %>
                <li><a href=<%= `/item/${item.item_id}` %>><%= item.item_name %></a></li>
            <%})%>
            </ul>
        <% } else { %>
            <p>No items found</p>
        <% } %>
        <a href="/item/new">Add new item</a>
        <br>
        <a href="/category">Back to categories</a>
</body>
</html>